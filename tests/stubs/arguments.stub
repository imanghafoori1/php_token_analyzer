<?php

class S
{
    function t(T1 $m1 = '', /** sdvf */ T2 $m2 = ['H', 0, null, ['d',], 's' => 's'], /* t */ T3 $m3) : T33
    {
	    fn ($path = ['a' => 'b']) => '1-' ? '2=' : '3_';
	    ['e' => fn ($path = ['a' => 'b']) => '1-' ? '2=' : '3_'];
        self::h($p, l();
        ['a' => T8::class];
    }
}
